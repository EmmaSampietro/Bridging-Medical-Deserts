# Core config & validation
omegaconf>=2.3.0
pydantic>=2.0,<3
PyYAML>=6.0

# Data
pandas>=2.0
pyarrow>=15.0.0

# HTTP (scraper)
httpx>=0.25.0

# Experiment tracking (optional; used by src.common.logging)
mlflow>=2.9.0

# UI/API (optional, for launch_ui.py api/streamlit modes)
fastapi>=0.110.0
uvicorn>=0.29.0
streamlit>=1.30.0

# Testing
pytest>=8.0.0
