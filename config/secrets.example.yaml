# Copy to secrets.yaml (or use env vars). Never commit real secrets.
# Required keys (inject via environment or .env):

# databricks:
#   host: ${DATABRICKS_HOST}
#   token: ${DATABRICKS_TOKEN}

# openai:
#   api_key: ${OPENAI_API_KEY}

# map_provider:
#   api_key: ${MAP_API_KEY}
