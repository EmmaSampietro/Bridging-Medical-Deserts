# Pipeline: ingest — VF CSV + optional scrape.
# Used by scripts/ingest_data.py

sources:
  vf_csv_path: data/raw
  country: Ghana
  scrape_enabled: true
  scrape_cache_dir: data/raw/scraped

scraper:
  timeout_sec: 30
  retries: 2
  respect_robots: true
  concurrency: 4
  user_agent: "BMD-Ingestor/0.1 (+https://example.org)"

chunker:
  max_chars: 900
  min_chars: 120
  overlap_chars: 120
